import 'package:flutter/material.dart';

class PlaydateScreen extends StatelessWidget {
  const PlaydateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Playdates'),
      ),
      body: const Center(
        child: Text('Playdate Screen - Coming Soon'),
      ),
    );
  }
} 